DB_ROOT_PASS=your_root_password_here
DB_NAME=a1083305
DB_USER=a1083305
DB_PASS=your_db_password_here
# Host port for the website. Leave unset for local dev (defaults to 8080).
# In production (with Caddy handling 80/443), set this to a different
# port, e.g. 8081, so it doesn't conflict with Caddy.
WEB_PORT=8080
# Google Analytics 4 Measurement ID. Leave unset for local dev so your
# own testing traffic doesn't get tracked. Set only in production.
GA_MEASUREMENT_ID=
